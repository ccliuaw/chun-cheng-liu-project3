Deployment

Now this project has been deployed on the vercel. You can play the sudoku games using link below:
link: https://chun-cheng-liu-project3.vercel.app/games


After running `npm install` (which you only need to do one time), to start your app, run the command `npm run dev`

Make sure to update the MongoDB string in server.js to start